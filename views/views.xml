<odoo>
  <data>
    <record model="ir.actions.act_window" id="shipment_list_action">
        <field name="name">Shipments</field>
        <field name="res_model">export_and_shipping.shipment</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem id="shipments_menu" name="Shipments" parent="sales_team.menu_sales"
          action="shipment_list_action"/>

    <!-- Shipments tree/list view -->
    <record model="ir.ui.view" id="shipments_tree_view">
        <field name="name">shipments.tree</field>
        <field name="model">export_and_shipping.shipment</field>
        <field name="arch" type="xml">
            <tree string="Shipments Tree">
                <field name="awb"/>
                <field name="departure_date"/>
                <field name="arrival_date"/>
                <field name="by_plane"/>
            </tree>
        </field>
    </record>
  </data>
</odoo>